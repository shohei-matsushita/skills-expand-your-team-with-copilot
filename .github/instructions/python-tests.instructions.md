---
applyTo: 'server/tests/test_*.py'
---

# テストに関する注意

- 下部のテストで共用するデータを、ファイル冒頭で作成する
- 正常系だけでなく、データが返らない場合のテストも含める
- データアクセスをテストする際はインメモリ SQLite を使用する
- セットアップ/ティアダウン関数を用いてテスト用 DB の作成と破棄を行う
    - `db.engine.dispose()` を呼び出し、DB を確実にクローズする
